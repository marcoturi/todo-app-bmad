$schema: "https://moonrepo.dev/schemas/project.json"

project:
    name: "todo-app-bmad"
    description: "Todo app monorepo root"

tasks:
    test:
        command: "pnpm test"

    test-coverage:
        command: "pnpm test:coverage"

    check:
        command: "pnpm check"

    build:
        command: "pnpm build"
